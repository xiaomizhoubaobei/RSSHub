interface:
  display_name: "RSSHub"
  short_description: "Repo-specific patterns and workflows for RSSHub"
  default_prompt: "Use the RSSHub repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true